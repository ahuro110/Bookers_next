<%= form_for(current_user.active_relationships.find_by(followed_id: user.id), url:user_relationships_path(user),
             html: { method: :delete }) do |f| %>
  <%= f.submit "フォローを外す", class: "btn" %>
<% end %>
